-- textwrap: wrapping, filling and indenting text for a terminal.
--
-- A port of Rust's `textwrap` crate, version 0.16.2, with its default
-- features: widths from `unicodeWidth`, word boundaries from
-- `unicodeLinebreak`, and optimal-fit wrapping. The tests hold it to the
-- crate's answers for thousands of texts and options.
--
--   use Baler (wrap, fill, options)
--
--   wrap (options 18) "Memory safety without garbage collection."
--     -- ["Memory safety", "without garbage", "collection."]

mod Tables
mod Str
mod Core
mod Options
mod Words
mod Algorithms
mod Wrap
mod Indentation
@cfg(test)
mod Cases
@cfg(test)
mod Tests

@pub use Baler.Options (
  Options,
  options,
  LineEnding,
  lineEndingText,
  WordSeparator,
  WordSplitter,
  WrapAlgorithm,
  Penalties,
  defaultPenalties
)
@pub use Baler.Options.LineEnding.*
@pub use Baler.Options.WordSeparator.*
@pub use Baler.Options.WordSplitter.*
@pub use Baler.Options.WrapAlgorithm.*
@pub use Baler.Wrap (wrap, fill, fillInplace, unfill, refill, wrapColumns)
@pub use Baler.Indentation (indent, dedent)
@pub use Baler.Core (displayWidth, Word, wordFrom, breakApart, breakWords, Fragment, fragment)
@pub use Baler.Words (findWords, splitPoints, splitWords)
@pub use Baler.Algorithms (wrapFirstFit, wrapOptimalFit)

[package]
name = "generate"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
textwrap = { version = "=0.16.2", default-features = false, features = ["smawk", "unicode-width", "unicode-linebreak"] }
unicode-linebreak = "=0.1.5"
unicode-width = "=0.2.2"

[build-dependencies]
serde_json = "1"
